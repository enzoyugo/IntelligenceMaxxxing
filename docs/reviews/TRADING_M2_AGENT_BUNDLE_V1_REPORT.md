# IM_M2_AGENT_BUNDLE@1.0.0

**Pre-registered before retrospective results.**  
**Manifest hash:** see `agent_bundle_v1.BUNDLE_MANIFEST_HASH`.

Frozen: schemas, feature allow/deny, thresholds, abstention rules, split boundaries, success/failure criteria, `live_policy_influence=false`.

Bug fixes require `1.0.1` + fresh run; no post-hoc threshold tuning.
