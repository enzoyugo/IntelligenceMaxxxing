# Trading Evidence Contract V1 Report

Observations must not include outcome/MFE/MAE/realized R. Features require `observed_at_utc` / `available_at_utc` ≤ cutoff. Evidence refs and reason codes are persisted on each assessment with input/output hashes.
