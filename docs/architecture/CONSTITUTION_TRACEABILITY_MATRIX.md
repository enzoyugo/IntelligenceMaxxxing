# CONSTITUTION TRACEABILITY MATRIX — STAGE 0

**Legend of states:** `IMPLEMENTED` · `PARTIALLY_IMPLEMENTED` · `CONTRACT_ONLY` · `DEFERRED` · `NOT_APPLICABLE`

Only what is protected by working code **and** a passing test is marked `IMPLEMENTED`.

| Constitutional principle | Architecture decision | Implementation | Protecting test | State |
|---|---|---|---|---|
| Engine independent of applications (Art. 4-A, Law 21) | Public API + external SDK + import boundaries; Core never imports apps/SDK | `src/intelligence_maxxxing/api/`, `sdk/python/`, `pyproject.toml` (import-linter) | `CORE_HAS_NO_APPLICATION_IMPORTS`, `SDK_USES_PUBLIC_HTTP_CONTRACT_ONLY` (tests/constitutional/test_import_boundaries.py) + import-linter contracts | IMPLEMENTED |
| Applications communicate only via public versioned contracts (Art. 4-A; Service Contract §7) | All endpoints under `/api/v1`; envelope with versions in `meta` | `api/app.py`, `contracts/api/envelope.py` | `PUBLIC_API_IS_VERSIONED` (test_boundary_protections.py) | IMPLEMENTED |
| Applications cannot access Engine DB directly (Art. 4-A) | ORM tables are internal; SDK speaks HTTP only; no DB credentials in contracts | `infrastructure/database/tables.py` (never exported publicly), SDK | `SDK_USES_PUBLIC_HTTP_CONTRACT_ONLY`; import-linter "Public SDK never imports the Engine Core" | IMPLEMENTED |
| Separation of epistemic states; inference never presented as fact (Art. 6) | `knowledge_class` mandatory, no default; observation endpoint rejects inference/conclusion classes | `domain/common/knowledge.py`, `contracts/api/observations.py` | `INFERENCE_CANNOT_DEFAULT_TO_FACT`, `test_conclusion_classes_cannot_enter_as_observations` (test_epistemic_contracts.py) | IMPLEMENTED |
| Right/duty to say "no sé" with reason (Art. 8; Epistemic §3) | `UNKNOWN` class requires `unknown_reason` enum | `domain/common/epistemic.py`, `domain/common/knowledge.py` | `UNKNOWN_REQUIRES_REASON` (test_epistemic_contracts.py) | IMPLEMENTED |
| Confidence decomposition (Epistemic §4.2) | `ConfidenceComponents` with data/method/conclusion/recommendation fields | `domain/common/confidence.py` | `test_components_are_separable` (tests/unit/test_domain_models.py) | IMPLEMENTED (structure only; no calibration math, honestly absent) |
| History is append-only; memory never rewritten (Art. 34, Law 9; Tech Arch §8) | Event store exposes append+read only; frozen domain events; DB uniqueness on aggregate version | `infrastructure/event_store/`, `application/ports/stores.py` | `EVENT_STORE_EXPOSES_NO_UPDATE`, `EVENT_STORE_EXPOSES_NO_DELETE`, `EVENTS_ARE_APPEND_ONLY`, `CURRENT_STATE_DOES_NOT_OVERWRITE_HISTORY` (test_append_only.py) | IMPLEMENTED |
| Every material action auditable and reconstructable (Governance §7) | `AuditRecord` per accepted write; `GET /api/v1/audits/{id}` returns record + events | `domain/audit/models.py`, `api/routes/audits.py` | `OBSERVATION_REQUIRES_AUDIT_ID`, `test_audit_is_recoverable` (integration) | IMPLEMENTED |
| Recommendations always carry audit ID (Service Contract §8) | `Recommendation.audit_id` required in schema | `domain/recommendations/models.py` | `RECOMMENDATION_SCHEMA_REQUIRES_AUDIT_ID` (test_epistemic_contracts.py) | CONTRACT_ONLY (no recommendations are produced yet) |
| Applications cannot mutate beliefs (Art. 4-A; Service Contract §6) | Frozen `Belief` model; no public belief-write endpoint exists | `domain/beliefs/models.py`, route table | `APPLICATION_CANNOT_MUTATE_BELIEF` (test_boundary_protections.py) | IMPLEMENTED (as an architectural guard; Belief Engine itself DEFERRED) |
| LLMs never write beliefs (Tech Arch §11) | No LLM integration exists; only write path is observations; beliefs frozen; `ActorType.LLM` reserved for attribution | route table, `domain/beliefs/models.py` | `LLM_CANNOT_WRITE_BELIEF` (test_boundary_protections.py) | IMPLEMENTED (as an architectural guard; LLM usage DEFERRED) |
| Idempotent writes; retries never duplicate (Service Contract §11) | Mandatory `Idempotency-Key`; payload-hash ledger; DB unique constraints; 409 on conflict | `application/use_cases/submit_observation.py`, `infrastructure/repositories/idempotency.py` | `IDEMPOTENT_RETRY_DOES_NOT_DUPLICATE`, `IDEMPOTENCY_PAYLOAD_CONFLICT_REJECTED` (test_idempotency.py) | IMPLEMENTED |
| No breaking change within a major version (Service Contract §7) | Committed JSON Schema snapshots for v1; structural diff detection | `contracts/schemas/registry.py`, `contracts/schemas/v1/*.json` | `BREAKING_SCHEMA_CHANGE_DETECTED` (test_schema_compatibility.py) | IMPLEMENTED |
| Constitution cannot change silently (Art. 58; Changes ledger) | SHA-256 manifest of frozen docs; reproducible verification | `docs/constitutional/CONSTITUTIONAL_MANIFEST.sha256`, `governance/manifest.py`, `scripts/audit/verify_constitution.ps1` | `CONSTITUTION_MANIFEST_MATCHES` (test_manifest.py) | IMPLEMENTED |
| Domain layer purity / layered monolith (Tech Arch §1, §4) | domain: pure Python; application: ports; infrastructure: SQLAlchemy; api: translation only | package layout + import-linter contracts | `DOMAIN_HAS_NO_FASTAPI_IMPORT`, `DOMAIN_HAS_NO_SQLALCHEMY_IMPORT`, `API_DOES_NOT_ACCESS_TABLES_DIRECTLY` (test_import_boundaries.py) | IMPLEMENTED |
| PostgreSQL central DB; SQLite for tests only (Tech Arch §3) | psycopg URL default; docker-compose Postgres; SQLite confined to test fixtures | `.env.example`, `docker-compose.yml`, `migrations/`, `tests/conftest.py` | `test_initial_migration_applies_from_scratch` (SQLite is used *by* tests, never as prod default) | IMPLEMENTED |
| Honest health reporting; degradation never hidden (Governance §6.4) | Component health checks (api, database, constitutional manifest); worst state wins | `api/routes/health.py`, `domain/common/health.py`, `infrastructure/health.py` | `test_health_without_database_reports_degradation` (integration) | IMPLEMENTED |
| Domain Packs never fake capabilities; contracts first (Domain Pack Standard §6) | `DomainPackContract` ABC; placeholders raise `CapabilityNotImplementedYet` | `domain_packs/base/contract.py`, `domain_packs/life/` | `test_life_pack_placeholder_never_simulates_capabilities` (test_domain_pack_honesty.py) | CONTRACT_ONLY |
| Permission scopes vocabulary (Service Contract §5) | `PermissionScope` enum declared; enforcement deferred (local private interface only) | `permissions/__init__.py` | — | CONTRACT_ONLY |
| Async jobs behind replaceable interface (Tech Arch §3; Service Contract §4.2) | `JobQueuePort` ABC; no queue implemented | `jobs/__init__.py` | — | CONTRACT_ONLY |
| Offline sync without duplication (Art. 43-A; Service Contract §9) | Server-side idempotency implemented; client queue contract documented | `docs/architecture/OFFLINE_SYNC_CONTRACT.md` + idempotency implementation | `IDEMPOTENT_RETRY_DOES_NOT_DUPLICATE` | PARTIALLY_IMPLEMENTED (server side done; client queue DEFERRED to Stage 3) |
| Hypothesis lifecycle (Art. 13) | `HypothesisStatus` enum + `Hypothesis` schema | `domain/hypotheses/models.py` | schema validation tests | CONTRACT_ONLY |
| Experiment pre-registration (Epistemic §10.1) | `Experiment` schema requires design fields before creation | `domain/experiments/models.py` | schema required-fields validation | CONTRACT_ONLY |
| Decision/outcome separation (Art. 19) | Distinct `Decision` and `Outcome` schemas; outcome references decision, never edits it | `domain/decisions/`, `domain/outcomes/` | frozen-model guarantees | CONTRACT_ONLY |
| Method lifecycle & promotion governance (Governance §4–5) | Not built; no method exists to govern | — | — | DEFERRED |
| Cross-Domain Coordinator (Art. 46; DPS §12) | Not built | — | — | DEFERRED |
| Autonomy levels & kill switch (Arts. 47–48) | Not built; Stage 0 has no execution capability at all (Level 0/1 only by construction) | — | — | DEFERRED |
| AI provider abstraction (Tech Arch §11) | No LLM/provider dependency exists anywhere in the codebase | — | absence enforced by dependency list | DEFERRED |
| Encryption at rest, secret manager, RBAC (Tech Arch §9) | Local development only; `.env` excluded from git; no secrets in code | `.gitignore`, `.env.example` | — | DEFERRED (required before any remote deployment) |
