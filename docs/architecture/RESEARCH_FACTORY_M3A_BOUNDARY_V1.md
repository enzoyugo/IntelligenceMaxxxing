# Research Factory M3A Boundary V1

- Namespace: `/api/v1/research/*` (not Stage 3 `/hypotheses` / `/experiments`)
- No TMX storage access
- No broker / OOS / policy mutation / auto-run
- Manual approval is the only path to MANUALLY_APPROVED
- M2 agents may emit DRAFT hypotheses / evidence types; never approvals
