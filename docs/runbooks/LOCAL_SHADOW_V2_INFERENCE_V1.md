# Local Shadow V2 Inference V1

1. Confirm TMX `shadow_v2_start_manifest.json` exists and `execution_enabled=false`.
2. Serve assessments with frozen FAMILY hashes only.
3. Do not apply V1 ledger aliases or backfill.
4. Forward path is observation-only — no training, calibration, or threshold updates.
